# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: "apiextensions.k8s.io/v1"
kind: "CustomResourceDefinition"
metadata:
  name: "flaisauthentications.novari.no"
  annotations:
{{- with .Values.annotations }}
{{- toYaml . | nindent 4 }}
{{- end }}
spec:
  group: "novari.no"
  names:
    kind: "FlaisAuthentication"
    plural: "flaisauthentications"
    singular: "flaisauthentication"
  scope: "Namespaced"
  versions:
  - name: "v1alpha1"
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              ingress:
                items:
                  properties:
                    host:
                      type: "string"
                    path:
                      type: "string"
                  required:
                  - "host"
                  type: "object"
                type: "array"
              realm:
                type: "string"
                x-kubernetes-validations:
                - message: "Invalid realm"
                  rule: "self in ['fint']"
              wonderwall:
                properties:
                  autoLogin:
                    type: "boolean"
                  logLevel:
                    type: "string"
                    x-kubernetes-validations:
                    - message: "Invalid logLevel, must be one of info, debug"
                      rule: "self in ['info', 'debug']"
                  scope:
                    items:
                      type: "string"
                    maxItems: 2
                    type: "array"
                    x-kubernetes-validations:
                    - message: "Invalid scope, must be one of profile, organization"
                      rule: "self.all(scope, scope in ['profile', 'organization'])"
                  upstreamPort:
                    type: "integer"
                required:
                - "upstreamPort"
                type: "object"
            required:
            - "realm"
            - "wonderwall"
            type: "object"
          status:
            type: "object"
        type: "object"
    served: true
    storage: true
